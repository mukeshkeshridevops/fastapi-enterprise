name: Security

on:
  push:

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          pip install bandit safety
      - run: |
          bandit -r app
      - run: |
          safety check
