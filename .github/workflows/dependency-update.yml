name: Dependency Check

on:
  schedule:
    - cron: "0 2 * * 1"

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          python -m pip install --upgrade pip
          pip list --outdated
